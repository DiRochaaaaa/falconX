# =============================================================================
# FALCONX - CONFIGURAÇÕES DE AMBIENTE
# =============================================================================
# IMPORTANTE: Nunca commite este arquivo com valores reais!
# Copie para .env.local e configure com suas credenciais reais.

# =============================================================================
# SUPABASE - DATABASE & AUTH
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-chave-anonima-aqui
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# URL base da aplicação
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Ambiente (development, staging, production)
NODE_ENV=development

# =============================================================================
# SECURITY & RATE LIMITING
# =============================================================================
# Chave secreta para JWT (gere uma nova em produção)
JWT_SECRET=sua-chave-jwt-super-secreta-aqui

# Chave secreta para geração de Script IDs únicos
SCRIPT_SECRET_KEY=sua-chave-secreta-para-scripts-2025

# Rate limiting (requests por minuto)
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# Rate limiting - quantas requests por minuto
RATE_LIMIT_PUBLIC_API=50
RATE_LIMIT_PROTECTED_API=200
RATE_LIMIT_CRITICAL_API=10

# Duração do bloqueio em minutos quando rate limit é atingido
RATE_LIMIT_BLOCK_DURATION_MINUTES=5

# URLs permitidas para CORS (separadas por vírgula)
ALLOWED_ORIGINS=https://falconx.com,https://app.falconx.com

# =============================================================================
# LOGGING & MONITORING
# =============================================================================
# Nível de log (debug, info, warn, error)
LOG_LEVEL=info

# Habilitar logs estruturados em produção
STRUCTURED_LOGS=false

# Habilitar funcionalidades beta
ENABLE_BETA_FEATURES=false

# Habilitar analytics
ENABLE_ANALYTICS=false

# Configurações de Alertas (para produção)
SLACK_WEBHOOK_URL=
SECURITY_ALERT_EMAIL=admin@falconx.com

# Configurações de Segurança (OPCIONAL)
ENABLE_SECURITY_AUDIT=true

# Configurações opcionais para produção
SENTRY_DSN=

# URL pública da sua aplicação (usado em VPS, EasyPanel, etc.)
NEXT_PUBLIC_APP_URL=https://app.falconx.com

# Configurações específicas do EasyPanel (opcional)
# PORT=3000
# HOST=0.0.0.0 